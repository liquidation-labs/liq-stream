websockets>=12.0,<13
websocket-client>=1.7.0
requests>=2.32.0
asyncpg>=0.29,<1
aiohttp>=3.9,<4        # if your Bybit/OKX discovery uses HTTP; keep if already present
uvloop>=0.19; platform_system!="Windows"   # optional perf on Linux